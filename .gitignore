# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo
# ...except the shared packages' compiled output, which the mobile app imports
# at runtime. EAS clones a clean tree (no local build), so this must be present
# for the Metro bundle to resolve @ludo/engine + @ludo/bot. The eas-build-post-
# install hook also rebuilds it, but committing keeps the build self-contained.
!packages/engine/dist/
!packages/bot/dist/

# Environment & secrets (NEVER commit)
.env
.env.*
!.env.example

# Expo / React Native
apps/mobile/.expo/
apps/mobile/web-build/
apps/mobile/ios/
apps/mobile/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Supabase
supabase/.branches/
supabase/.temp/
# Generated: engine + bot copied for Deno (run `npm run build:edge`)
supabase/functions/_shared/engine/
supabase/functions/_shared/bot/
# Written by `npm run typecheck:edge`; the pinned deno version is the contract
deno.lock

# Logs
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Test / coverage
coverage/

# OS / editor
.DS_Store
.idea/
.vscode/
.history/
*.swp

# Qodo (unused)
.qodo/

# Expo/EAS config belongs to the app, not the workspace root. Running `expo` or
# `eas` from here mints stub /app.json + /eas.json files that shadow the real
# ones in apps/mobile and break `require("../../app.json")` in SettingsScreen.
/app.json
/eas.json
