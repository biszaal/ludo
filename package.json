{
  "name": "ludo",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time multiplayer Ludo game (React Native + Supabase). The game engine is the source of truth; everything else is a projection of state.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm test --workspace packages/engine && npm test --workspace packages/bot && npm test --workspace mobile && npm run test:edge",
    "test:engine": "npm test --workspace packages/engine",
    "test:bot": "npm test --workspace packages/bot",
    "test:app": "npm test --workspace mobile",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "typecheck:edge": "npm run build:edge && npx --yes deno@2.1.4 check supabase/functions/game/index.ts supabase/functions/ads-ssv/index.ts supabase/functions/rc-webhook/index.ts",
    "build:engine": "npm run build --workspace packages/engine",
    "build:bot": "npm run build --workspace packages/bot",
    "build:packages": "npm run build:engine && npm run build:bot",
    "build:edge": "npm run build:packages && node scripts/build-edge.mjs",
    "mobile": "npm start --workspace mobile",
    "ios": "npm run run:ios --workspace mobile",
    "android": "npm run run:android --workspace mobile",
    "test:edge": "SUPABASE_URL=https://example.supabase.co SUPABASE_SERVICE_ROLE_KEY=test npx --yes deno@2.1.4 test --no-check --allow-env --allow-net supabase/functions/game/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "expo": "~56.0.12",
    "react": "19.2.3",
    "react-native": "0.85.3"
  }
}
